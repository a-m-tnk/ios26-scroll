import type { IconProps } from '@platform-ui/icon';
// eslint-disable-next-line no-restricted-imports
import UIIcon from '@platform-ui/icon';

export const Icon = (props: IconProps) => {
    return <UIIcon {...props} />;
};
